// A FIGlet generation library - MIT license
// https://github.com/picrap/FIGlet

namespace FIGlet
{
    /// <summary>
    /// To mark Unicode chars (which is better than <see cref="char"/>)
    /// I had no better idea than doing this
    /// </summary>
    public enum UnicodeChar: int
    {
    }
}